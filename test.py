from atguigu.tool.get_bgem3 import get_embedding

a = []
for i in a:
    print(type(i))
    print(1)
    print(i[0])
    # embeddings = get_embedding([i])
    # print(embeddings)
    # dense = embeddings.get("dense")[0]
    # print(dense)
embeddings = get_embedding([])
# print(embeddings)
# for idx, item_name in enumerate([]):
#     print(1)
#     # 获取到每个item_name对应的稠密和稀疏
#     dense = embeddings.get("dense")[idx]
#     sparse = embeddings.get("sparse")[idx]
